import React from "react";
import ReactDOM from "react-dom/client";
import App from "./components/App";
   
const bodyRoot = ReactDOM.createRoot(document.getElementById("root"));
bodyRoot.render(
    <App/>
);
